# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project was downgraded from SDK 57 to SDK 54 on 2026-07-30 so it can run in the
published Expo Go app (Expo Go only supports one fixed SDK at a time, and 57 wasn't supported
by the installed client). If you ever bump the SDK again, re-check Expo Go's currently supported
version first.
